window.revelar = ScrollReveal({ reset: false });
  
  var slideUp = {
    distance: '200px',
    origin: 'bottom',
    opacity: 1,
    interval: 200,
    
  };

  ScrollReveal().reveal('.titulo-reveal', slideUp);
